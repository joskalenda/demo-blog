<h2>Comments</h2>
<div id ="comments">
  <%# Expands to renderr partial: 'comments/comment', collection: post.comments %>
  <%= render post.comments %>
</div>

<%= render 'comments/new', post: post %>